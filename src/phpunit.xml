<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/schema/9.2.xsd"
         processIsolation="true"
         cacheResult="false"
         colors="true"
         testdox="true">
    <testsuites>
        <testsuite name="Unit">
            <!-- Testing smallest units or modules individually. -->
            <!-- The unit test ensures that individual code i.e. small piece of code works fine. -->
            <directory suffix="Test.php">./tests/Unit/</directory>
        </testsuite>
        <testsuite name="Integration">
            <!-- Testing integration of two or more units/modules combined for performing tasks. -->
            <!-- An integration test is done to ensure that - when one or more units are integrated, it works fine. -->
            <directory suffix="Test.php">./tests/Integration/</directory>
        </testsuite>
        <testsuite name="Functional">
            <!-- Testing the behavior of the application as per the requirement. -->
            <directory suffix="Test.php">./tests/Functional/</directory>
        </testsuite>
        <testsuite name="Feature">
            <!-- The feature test is the testing of features just like an actual user. Like how the actual user will use the feature. -->
            <directory suffix="Test.php">./tests/Feature/</directory>
        </testsuite>
    </testsuites>
</phpunit>